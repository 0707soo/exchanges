name: update-rates-legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy workflow disabled. Use rates-burst-sync.'
